[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Constrained LLM decoding engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy<=2.5.2",
    "pydantic<=2.13.5",
    "llm_sdk",
]

[tool.uv.sources]
llm-sdk = { path = "./llm_sdk" }
